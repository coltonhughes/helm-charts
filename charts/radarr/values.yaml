replicaCount: 1

deploymentStrategy:
  type: Recreate

image:
  repository: linuxserver/radarr
  pullPolicy: Always
  tag: "latest"

container:
  env:
    TZ: America/Chicago
    PUID: "1000"
    PGID: "1000"

persistence:
  storageClass: ""
  enabled: true
  existingClaim: ""
  accessMode: ReadWriteOnce
  size: 2Gi
  mountPath: /config
  name: config

service:
  type: ClusterIP
  port: 7878

ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/tls-acme: "true"
  hosts:
    - host: radarr.test.articops.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: radarr-test-articops-tls
      hosts:
        - radarr.test.articops.com

volumes: []
volumeMounts: []
